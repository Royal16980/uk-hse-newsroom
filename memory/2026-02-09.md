# 2026-02-09 — Daily self-improvement sweep

## Inputs scanned
- memory: 2026-02-08, 2026-02-06
- .learnings: LEARNINGS.md, ERRORS.md, FEATURE_REQUESTS.md

## Promotions / updates
### TOOLS.md
- Added Windows `exec`/PowerShell separator gotcha: use `;` + `if {}` instead of `&&`/`||`.

### .learnings/ERRORS.md
- Logged new error: `openclaw status` and `openclaw gateway status` hang when run via `exec` (killed).

### .learnings/FEATURE_REQUESTS.md
- Normalized file to a simple header + “empty for now” (it was showing as modified despite being effectively empty).

## Quick health checks
- n8n API (list workflows): OK (100 workflows returned).
- n8n webhook `/webhook/bsmb-events`: OK (POST returned HTTP 200 `{ "ok": true }`).
- OpenClaw gateway/status CLI: NOT OK (hangs via `exec`; see ERR-20260209-001).

## Repo status
- Workspace has multiple modified/untracked files beyond today’s sweep changes (existing WIP). Avoided committing broad WIP without review.

## Next actions
1) Decide how you want me to handle commits in this repo going forward:
   - (A) commit only sweep-related changes in small scoped commits, even if repo is dirty
   - (B) keep sweep changes uncommitted until you say “commit everything”
2) For OpenClaw health checks: try `openclaw status` with `pty=true` in a future sweep; if still hanging, implement a fallback liveness check (Task Scheduler + port check).
