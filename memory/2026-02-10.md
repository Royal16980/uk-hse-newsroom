# 2026-02-10

## Model deprecation incident (06:38–07:58 GMT)
- `claude-opus-4-5` was retired by Anthropic overnight.
- Every API call (main session, cron jobs) returned: "Claude Opus 4.5 is no longer available. Please switch to Claude Opus 4.6."
- Royal sent ~8 messages over 1+ hour with zero useful response — all got the same error.
- The config wizard ran at 07:03 and updated primary model to `anthropic/claude-opus-4-6`, but the session kept returning cached errors until ~07:58 when the new model finally kicked in.

## Fixes applied
1. **Main session**: Now running on `claude-opus-4-6` (confirmed working).
2. **Self-improvement cron job**: Was using `openai-codex/gpt-5.2` (error: "model not allowed"). Updated to `anthropic/claude-sonnet-4-5`.
3. **Daily digest cron job**: Uses default model, inherits `claude-opus-4-6` — ran OK.
4. **Sub-agents** (writer/research/coder/ops): Still set to `openai-codex/gpt-5.2` — testing if they work via spawn.

## Hierarchy fix (19:10 GMT)
- Royal flagged: Savannah was doing ops' job (dispatching agents directly). Wasting tokens.
- Fixed config: main → ops only. Ops → writer, research, coder, finance.
- Ops dispatch cron now spawns ops agent, which then spawns workers. Proper chain of command.
- Cron runs at 08:00, 12:00, 16:00, 20:00 Europe/London.

## Deliverables completed today
- Gateway runbook + healthcheck + evidence (coder)
- Metrics instrumentation: KPI sheet, baseline, review runbook (coder)
- Webhook security audit (coder) — auth broken, fix documented
- Lead magnet: 30-day SME H&S checklist + CTA blocks (writer)
- RA template pack: timed out first attempt, re-dispatched (second writer job status unknown)
- GitHub decision logged: PR-only, no direct-to-main
- Ops daily checklist created

## Lessons logged
- Model deprecation can silently break everything — cron, main session, sub-agents.
- Need a resilience strategy: fallback models, alerts when errors spike.
- anthropic/claude-sonnet-4-5 returns "model not allowed" in cron jobs — use default model or remove override.
- Don't micromanage: delegate to ops, ops delegates to workers. Savannah = strategy + Royal comms only.
